export const toInt=(t,r=0)=>Number.isFinite(Number(t))?Math.round(Number(t)):Math.round(Number(r)||0);export const toPosInt=(t,r=0)=>Math.max(Math.round(Number(r)||0),toInt(t,r));export const toTrimmed=t=>String(t??"").trim();export const evalExpr=(t,r)=>{const e=String(t??"").trim();if(!e)return r;const n=Number(e.replace(",","."));if(Number.isFinite(n))return n;const o=e.replace(/,/g,".").replace(/\s+/g,"");if(!/^[0-9+\-*/().]+$/.test(o))return r;try{const t=Function(`"use strict";return (${o});`)();return Number.isFinite(t)?t:r}catch{return r}};export const toRoundedInt=(t,r=0)=>{const e=Number(t);if(Number.isFinite(e))return Math.round(e);const n=Number(r);return Number.isFinite(n)?Math.round(n):0};export const clampInt=(t,r,e,n=r)=>Math.max(r,Math.min(e,toRoundedInt(t,n)));export const toPositiveInt=(t,r=1)=>Math.max(1,toRoundedInt(t,r));export const parseAreaM2PxInput=(t,r)=>{const e=String(t??"").trim();if(!e)return Math.max(1,Math.round(Number(r)||65536));const n=e.match(/^\s*(\d+(?:[.,]\d+)?)\s*[xх×*]\s*(\d+(?:[.,]\d+)?)\s*$/i);if(n){const t=Number(String(n[1]).replace(",","."))*Number(String(n[2]).replace(",","."));if(Number.isFinite(t)&&t>0)return Math.max(1,Math.round(t))}const o=evalExpr(e,r),u=Number(o);return!Number.isFinite(u)||u<=0?Number(r)||65536:Math.max(1,Math.round(u))};